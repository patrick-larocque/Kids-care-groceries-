// Re-export generated Supabase types after running: pnpm --filter @kids-care/db generate-types
// export type { Database } from './database.types';

// Convenience role type used across apps
export type UserRole = 'central_admin' | 'house_staff';
